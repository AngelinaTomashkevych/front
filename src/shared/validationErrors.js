export const getMaxLengthMessage = (number) =>
  `Max length ${number} characters`;
export const getMinLengthMessage = (number) =>
  `Min length ${number} characters`;

export const ERRORS_MASSAGES = {
  REQUIRED: 'Required field',
};
